name: push events
on: 
  push:
    branches:
      - main
      
jobs:
  show_info:
    runs-on: ubuntu-latest
    steps:
      - name: info
        run: |
              echo "this is push event on all branches"
